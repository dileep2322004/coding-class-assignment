#include<stdio.h>
void main()
{
	char a[30];
	int i,j,flag=0,n=0;
	scanf("%s",a);
	for(i=0;a[i]!='\0';i++)
	n++;
	for(i=n-1;j=0;i>=(n/2);i--;j++)
	{
		if(a[j]!=a[i])
		{
			flag=1;
			break;
		}
	}
	if(flag==0)
	printf("%s is palindrome",a[i]);
	else
	printf("not palindrome");
}
